const Cat = require('../../models/productCategoryModel')
const Error = require('../../utils/errorResponse')

const createCategory = (req, res, next) => {
  const { name, briefDetails, mainPhoto } = req.body
}
